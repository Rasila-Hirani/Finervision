import styled from "styled-components";
import { COLORS } from '../../variables';

export const AccordionStyle = styled.ul`
list-style: none;
    margin: 0;
    padding: 0;
    border-radius:${COLORS.radius};
    border: none;
    background: #fff;
    box-shadow: 5px 5px 5px  rgba(68, 68, 68, 0.6);
`;

